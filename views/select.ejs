<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>曲ごとのコンボ数</h2>
  <table>
      <% for( let row of data ) { %>
        <tr>
            <td><%= row.title %></td>
            <td><%= row.level %></td>
            <td><%= row.combo %></td>
        </tr>
      <% } %>
  </table>
</body>
</html>